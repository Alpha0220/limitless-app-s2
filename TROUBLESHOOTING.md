# 🔧 คู่มือแก้ไขปัญหา - Queue Master

## ❌ ปัญหา: Network Timeout (ETIMEDOUT)

### อาการ
- Error: `ETIMEDOUT` หรือ `request to https://api.airtable.com failed`
- ระบบ retry 3 ครั้งแล้วแต่ยังไม่สำเร็จ
- ไม่สามารถบันทึกข้อมูลลง Airtable ได้

### สาเหตุที่เป็นไปได้

1. **WSL2 Network Configuration**
   - WSL2 อาจมีปัญหาในการเชื่อมต่อ network
   - DNS resolution อาจไม่ทำงาน

2. **Firewall/Proxy Blocking**
   - Firewall อาจ block Airtable API
   - Proxy server อาจมีปัญหา

3. **Internet Connection**
   - การเชื่อมต่ออินเทอร์เน็ตไม่เสถียร
   - Network latency สูง

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: ตรวจสอบการเชื่อมต่อ

```bash
# ทดสอบการเชื่อมต่อ Airtable API
curl -I https://api.airtable.com

# ทดสอบ DNS
nslookup api.airtable.com
```

### วิธีที่ 2: แก้ไข WSL2 Network (ถ้าใช้ WSL2)

```bash
# ตรวจสอบ DNS
cat /etc/resolv.conf

# ถ้า DNS ไม่ทำงาน ลองแก้ไข:
sudo bash -c 'echo "nameserver 8.8.8.8" > /etc/resolv.conf'
sudo bash -c 'echo "nameserver 8.8.4.4" >> /etc/resolv.conf'
```

### วิธีที่ 3: ใช้ Windows Host Network (WSL2)

ถ้าใช้ WSL2 ลองใช้ Windows network:

```bash
# ดู IP ของ Windows host
cat /etc/resolv.conf | grep nameserver

# ใช้ IP นั้นเป็น DNS
```

### วิธีที่ 4: ตรวจสอบ Proxy/Firewall

1. ตรวจสอบว่ามี Proxy อยู่หรือไม่:
   ```bash
   echo $HTTP_PROXY
   echo $HTTPS_PROXY
   ```

2. ถ้ามี Proxy ต้องตั้งค่าให้ WSL2 ใช้

### วิธีที่ 5: เพิ่ม Timeout และ Retry

โค้ดมี retry logic อยู่แล้ว แต่ถ้ายังมีปัญหา ลองเพิ่ม timeout:

แก้ไข `app/api/bookings/route.ts`:
```typescript
const base = new Airtable({
  apiKey: process.env.AIRTABLE_API_KEY,
  requestTimeout: 60000, // เพิ่มเป็น 60 วินาที
}).base(process.env.AIRTABLE_BASE_ID || '');
```

### วิธีที่ 6: ทดสอบจาก Browser

ลองเปิดเบราว์เซอร์และไปที่:
- https://api.airtable.com/v0/appfHouIztFByiTpa/Bookings

ถ้าเปิดไม่ได้ แสดงว่ามีปัญหา network

---

## 🔍 การวินิจฉัยปัญหา

### ขั้นตอนที่ 1: ตรวจสอบ Network

```bash
# ทดสอบ ping
ping -c 4 api.airtable.com

# ทดสอบ curl
curl -v https://api.airtable.com
```

### ขั้นตอนที่ 2: ตรวจสอบ Environment Variables

```bash
# ตรวจสอบว่า .env.local มีข้อมูลครบ
cat .env.local
```

### ขั้นตอนที่ 3: ตรวจสอบ Airtable API Key

1. ไปที่ https://airtable.com/api
2. เลือก Base "Queue Master"
3. ตรวจสอบว่า API Key ยังใช้งานได้

---

## 💡 วิธีแก้ไขชั่วคราว

### ใช้ Mock Data (สำหรับ Development)

ถ้ายังแก้ปัญหา network ไม่ได้ สามารถใช้ mock data ชั่วคราว:

1. สร้าง API route ใหม่ที่ return mock data
2. หรือใช้ local storage แทน Airtable

### ใช้ VPN

ถ้า Airtable API ถูก block ลองใช้ VPN

---

## 📞 ขั้นตอนต่อไป

1. **ตรวจสอบ Network**: รันคำสั่งใน "การวินิจฉัยปัญหา"
2. **แก้ไขตามวิธีที่เหมาะสม**: เลือกวิธีที่เหมาะกับสภาพแวดล้อมของคุณ
3. **ทดสอบอีกครั้ง**: รัน `pnpm run dev` และลองบันทึกข้อมูล

---

## ⚠️ หมายเหตุ

- ปัญหา Network Timeout มักเกิดจาก WSL2 network configuration
- ถ้าใช้ Windows + WSL2 อาจต้อง restart WSL2:
  ```powershell
  # ใน PowerShell (Windows)
  wsl --shutdown
  # แล้วเปิด WSL2 ใหม่
  ```

---

## 📚 เอกสารเพิ่มเติม

- [WSL2 Network Issues](https://docs.microsoft.com/en-us/windows/wsl/networking)
- [Airtable API Documentation](https://airtable.com/api)

