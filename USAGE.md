# 📖 คู่มือการใช้งาน Queue Master

## 🚀 เริ่มต้นใช้งาน

### 1. รันแอปพลิเคชัน

```bash
pnpm run dev
```

เปิดเบราว์เซอร์ไปที่: **http://localhost:3000**

---

## 📋 ขั้นตอนการใช้งาน

### หน้าแรก: เลือกช่วงเวลาและห้อง

1. **เลือกช่วงเวลา**
   - เลือกจาก dropdown "เลือกช่วงเวลา"
   - มี 6 ช่วงเวลาให้เลือก:
     - 10:00 - 12:00
     - 12:00 - 14:00
     - 14:00 - 16:00
     - 16:00 - 18:00
     - 18:00 - 20:00
     - 20:00 - 22:00

2. **เลือกห้อง**
   - หลังจากเลือกช่วงเวลาแล้ว จะแสดงห้องที่ว่างในช่วงเวลานั้น
   - คลิกที่ห้องที่ต้องการจอง
   - ห้องที่เลือกจะถูกไฮไลต์

3. **ดำเนินการต่อ**
   - กดปุ่ม "ดำเนินการต่อ →" เพื่อไปยังหน้าฟอร์มจอง

---

### หน้าฟอร์มจอง: กรอกข้อมูล

1. **ข้อมูลการจองที่เลือก**
   - แสดงช่วงเวลาและห้องที่เลือกไว้ (ไม่สามารถแก้ไขได้)

2. **กรอกข้อมูลส่วนตัว**
   - **ชื่อ** (บังคับ)
   - **นามสกุล** (บังคับ)

3. **แนบใบเสร็จ** (ไม่บังคับ)
   - คลิก "เลือกไฟล์" เพื่ออัปโหลดรูปใบเสร็จ
   - ระบบจะแสดงตัวอย่างรูปภาพ
   - **หมายเหตุ:** การอัปโหลดใบเสร็จจะพร้อมใช้งานในอนาคต

4. **บันทึกข้อมูล**
   - กดปุ่ม "บันทึกข้อมูล"
   - ระบบจะบันทึกลง Airtable
   - หากสำเร็จ จะแสดงหน้าสำเร็จและกลับไปหน้าแรกอัตโนมัติ

---

## ⚠️ ข้อควรทราบ

### ข้อมูลที่บันทึกใน Airtable

- ✅ ชื่อ
- ✅ นามสกุล
- ✅ ช่วงเวลา
- ✅ Room ID
- ✅ Room Name
- ✅ Status (ตั้งเป็น "Pending" อัตโนมัติ)
- ✅ Created At (วันที่และเวลาที่บันทึก)
- ⏳ Receipt (ยังไม่พร้อมใช้งาน - ต้องใช้ file storage service)

### การทำงานของระบบ

- **Retry Logic**: ระบบจะ retry อัตโนมัติ 3 ครั้งเมื่อเกิด network timeout
- **Error Handling**: แสดงข้อความ error ที่เข้าใจง่ายเป็นภาษาไทย
- **Session Storage**: ใช้ sessionStorage เพื่อส่งข้อมูลระหว่างหน้า

---

## 🔧 การแก้ไขปัญหา

### ปัญหา: "การเชื่อมต่อ timeout"

**สาเหตุ:** Network timeout ระหว่างเชื่อมต่อกับ Airtable

**วิธีแก้:**
1. ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
2. ระบบจะ retry อัตโนมัติ 3 ครั้ง
3. ลองอีกครั้งหลังจากรอสักครู่

### ปัญหา: "ไม่ได้รับอนุญาตให้เข้าถึง Airtable"

**สาเหตุ:** Personal Access Token ไม่มีสิทธิ์

**วิธีแก้:**
1. ตรวจสอบว่า Personal Access Token มี Scopes:
   - `data.records:read`
   - `data.records:write`
   - `schema.bases:read`
2. ตรวจสอบว่า Access เป็น "ALL RESOURCES"

### ปัญหา: ข้อมูลไม่ถูกบันทึก

**วิธีแก้:**
1. ตรวจสอบว่า Fields ใน Airtable ครบ 8 ตัว
2. ตรวจสอบว่า Field names ตรงกับที่ระบุไว้ทุกตัวอักษร
3. ตรวจสอบ Console log ใน terminal เพื่อดู error message

---

## 📊 ตรวจสอบข้อมูลใน Airtable

1. ไปที่ Airtable: https://airtable.com
2. เปิด Base "Queue Master"
3. เปิด Table "Bookings"
4. ตรวจสอบว่ามีข้อมูลการจองถูกบันทึกหรือไม่

---

## 🎯 Features ที่พร้อมใช้งาน

- ✅ เลือกช่วงเวลาและห้อง
- ✅ กรอกข้อมูลการจอง
- ✅ บันทึกลง Airtable
- ✅ Retry logic สำหรับ network issues
- ✅ Error handling ที่ดี
- ✅ UI/UX ที่สวยงาม

## 🔮 Features ที่จะเพิ่มในอนาคต

- ⏳ อัปโหลดใบเสร็จ (ต้องใช้ file storage service)
- ⏳ ตรวจสอบห้องว่างจาก Airtable แทน mock data
- ⏳ Email notification
- ⏳ Admin dashboard

---

## 📞 ติดต่อ

หากมีปัญหาหรือคำถาม กรุณาตรวจสอบ:
- `README.md` - ข้อมูลทั่วไป
- `QUICK_START.md` - คู่มือเริ่มต้นใช้งาน
- `AIRTABLE_SETUP.md` - คู่มือตั้งค่า Airtable

