# 🎯 คู่มือตั้งค่า Airtable แบบง่ายๆ

## สิ่งที่คุณต้องทำเอง (ฉันทำแทนไม่ได้)

### 1. สร้าง Airtable Base
- ไปที่ https://airtable.com
- ล็อกอินด้วยอีเมลของคุณ
- สร้าง Base ใหม่ชื่อ "Queue Master" (หรือชื่ออื่น)
- **สำคัญ:** ต้องสร้าง Base เอง เพราะ Airtable API ไม่รองรับการสร้าง Base ใหม่

### 2. สร้าง Table
- ใน Base ที่สร้างไว้ จะมี Table เริ่มต้นชื่อ "Table 1"
- เปลี่ยนชื่อเป็น **"Bookings"**

### 3. หา API Key และ Base ID
- ไปที่ https://airtable.com/api
- เลือก Base ที่สร้างไว้
- คัดลอก **API Key** และ **Base ID**

## สิ่งที่ฉันจะช่วยทำให้

เมื่อคุณส่ง **API Key** และ **Base ID** มาให้ฉัน ฉันจะ:

1. ✅ สร้างไฟล์ `.env.local` ให้พร้อมใช้งาน
2. ✅ ตั้งค่า environment variables ทั้งหมด
3. ✅ สร้าง script สำหรับตรวจสอบและทดสอบการเชื่อมต่อ

## ขั้นตอนหลังจากส่ง API Key และ Base ID มา

### วิธีที่ 1: ให้ฉันช่วยตั้งค่า (แนะนำ)

1. ส่ง API Key และ Base ID มาให้ฉัน
2. ฉันจะสร้างไฟล์ `.env.local` ให้
3. คุณรัน `npm run setup:airtable` เพื่อตรวจสอบ Fields
4. ถ้า Fields ยังไม่ครบ script จะบอกว่าต้องสร้าง Fields อะไรบ้าง

### วิธีที่ 2: ตั้งค่าเอง

1. สร้างไฟล์ `.env.local`:
   ```bash
   cp .env.example .env.local
   ```

2. แก้ไข `.env.local`:
   ```env
   AIRTABLE_API_KEY=your_api_key_here
   AIRTABLE_BASE_ID=your_base_id_here
   AIRTABLE_TABLE_NAME=Bookings
   NEXT_PUBLIC_APP_URL=http://localhost:3000
   ```

3. สร้าง Fields ใน Table "Bookings":
   - ดูรายละเอียดใน `QUICK_START.md`

4. ทดสอบ:
   ```bash
   npm run setup:airtable
   ```

## Fields ที่ต้องสร้างใน Table "Bookings"

| Field Name | Type | Options |
|------------|------|---------|
| First Name | Single line text | - |
| Last Name | Single line text | - |
| Time Slot | Single line text | - |
| Room ID | Single line text | - |
| Room Name | Single line text | - |
| Receipt | Attachment | - |
| Status | Single select | Pending, Confirmed, Cancelled |
| Created At | Date | Include time: Yes |

**⚠️ สำคัญ:** ชื่อ Field ต้องตรงกับที่ระบุไว้ทุกตัวอักษร (Case-sensitive)

## สรุป

**ฉันทำแทนไม่ได้:**
- ❌ สร้าง Airtable Base (ต้องทำผ่านเว็บ)
- ❌ สร้าง Table (ต้องทำผ่านเว็บ)

**ฉันทำให้ได้:**
- ✅ ตั้งค่า `.env.local` เมื่อคุณให้ API Key และ Base ID มา
- ✅ สร้าง script สำหรับตรวจสอบและทดสอบ
- ✅ ช่วยแก้ไขโค้ดและ configuration

**ส่ง API Key และ Base ID มาได้เลยครับ!** 🚀

