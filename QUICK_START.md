# 🚀 คู่มือเริ่มต้นใช้งาน Queue Master

## ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
npm install
```

## ขั้นตอนที่ 2: สร้าง Airtable Base และ Table

### 2.1 สร้าง Base ใน Airtable

1. ไปที่ [https://airtable.com](https://airtable.com)
2. ล็อกอินด้วยอีเมล: **nattapong05032535@gmail.com**
3. คลิก **"Add a base"** → เลือก **"Start from scratch"**
4. ตั้งชื่อ Base: **"Queue Master"**

### 2.2 สร้าง Table และ Fields

1. เปลี่ยนชื่อ Table จาก "Table 1" เป็น **"Bookings"**

2. เพิ่ม Fields ตามนี้ (คลิก "+ Add a field" สำหรับแต่ละ field):

   ```
   Field Name: First Name
   Type: Single line text
   
   Field Name: Last Name
   Type: Single line text
   
   Field Name: Time Slot
   Type: Single line text
   
   Field Name: Room ID
   Type: Single line text
   
   Field Name: Room Name
   Type: Single line text
   
   Field Name: Receipt
   Type: Attachment
   
   Field Name: Status
   Type: Single select
   Options: Pending, Confirmed, Cancelled
   
   Field Name: Created At
   Type: Date
   Include time: Yes
   ```

   **⚠️ สำคัญ:** ชื่อ Field ต้องตรงกับที่ระบุไว้ทุกตัวอักษร (Case-sensitive)

## ขั้นตอนที่ 3: หา API Key และ Base ID

### 3.1 หา API Key

1. ไปที่ [https://airtable.com/api](https://airtable.com/api)
2. เลือก Base "Queue Master"
3. คลิก **"Generate API key"** (ถ้ายังไม่มี)
4. **คัดลอก API Key** ที่แสดง

### 3.2 หา Base ID

1. ยังอยู่ในหน้า API documentation
2. **Base ID** จะแสดงอยู่ด้านบน (รูปแบบ: `appXXXXXXXXXXXXXX`)
3. หรือดูจาก URL: `https://airtable.com/api/appXXXXXXXXXXXXXX`
4. **คัดลอก Base ID**

## ขั้นตอนที่ 4: ตั้งค่า Environment Variables

1. สร้างไฟล์ `.env.local` ในโฟลเดอร์โปรเจกต์:

```bash
cp .env.example .env.local
```

2. เปิดไฟล์ `.env.local` และแก้ไข:

```env
AIRTABLE_API_KEY=paste_your_api_key_here
AIRTABLE_BASE_ID=paste_your_base_id_here
AIRTABLE_TABLE_NAME=Bookings
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

3. แทนที่:
   - `paste_your_api_key_here` → API Key ที่ได้จากขั้นตอนที่ 3.1
   - `paste_your_base_id_here` → Base ID ที่ได้จากขั้นตอนที่ 3.2

## ขั้นตอนที่ 5: ทดสอบการเชื่อมต่อ

รันคำสั่งนี้เพื่อทดสอบว่าเชื่อมต่อ Airtable ได้หรือไม่:

```bash
npm run test:airtable
```

ถ้าเห็นข้อความ **"🎉 ทุกอย่างทำงานได้ปกติ!"** แสดงว่าพร้อมใช้งานแล้ว

## ขั้นตอนที่ 6: รันแอป

```bash
npm run dev
```

เปิดเบราว์เซอร์ไปที่: [http://localhost:3000](http://localhost:3000)

## ✅ ตรวจสอบว่าทำงานได้

1. เลือกช่วงเวลาและห้องในหน้าแรก
2. กรอกข้อมูลในหน้าจอง
3. อัปโหลดรูปใบเสร็จ
4. กดบันทึก
5. ตรวจสอบใน Airtable ว่ามีข้อมูลถูกบันทึกหรือไม่

## 🆘 ถ้ามีปัญหา

ดูไฟล์ `AIRTABLE_SETUP.md` สำหรับคำแนะนำเพิ่มเติม

